# Assistant Card Verification

The PlanPerm assistant is rendered in its own keyed workspace container with a 269-pixel desktop width, starting alongside the map rather than enclosing the page. The container carries the dedicated gradient, border, radius, and shadow treatment while the broad Streamlit wrappers remain transparent and borderless.

The selected-site line above the map has no bottom border. The small header theme control remains separate from the assistant card, preserving the requested open page composition.
