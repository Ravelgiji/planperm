"""Dependency-light primitives shared by the agents and views.

Most of this package was superseded by `agents/helpers.py`, which carries
ported copies of the planning, evidence, registry and requirements helpers.
`watch.py` is the exception and is live: it holds the source-index comparison
the watch agent is built on.
"""
