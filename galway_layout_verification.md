# Galway-first compact layout verification

The initial Streamlit state now opens at **Galway, Ireland** with a 2 km radius. The first live lookup returned 220 nearby applications, an 83% approval rate, and 30 refused applications. The oversized `Plan with the local record.` headline has been removed; the terrain banner now contains a concise local-record sentence and the three planning metrics.

The map can take a short time to render after the planning lookup completes. The selected-site bar and compact left-side controls render immediately while the embedded map hydrates.
